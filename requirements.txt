# Dashboard runtime only. The offline data-prep scripts (prepare_dataset.py,
# prepare_ignitions.py) additionally need geopandas, shapely and scipy —
# install those separately, they are not needed to serve the app.
streamlit>=1.28,<2
plotly>=5.24,<8
pandas>=2.0,<3
pyarrow>=14.0,<22
